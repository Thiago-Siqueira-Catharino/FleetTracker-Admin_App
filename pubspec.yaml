name: fleettracker_admin_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^8.3.0
  http: ^1.6.0
  latlong2: ^0.9.1
  shared_preferences: ^2.5.5
  nfc_manager: ^4.2.1
  nfc_manager_ndef: ^1.1.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
